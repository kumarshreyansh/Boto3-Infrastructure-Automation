# Track IAM access keys stored on all instances of a particular region in AWS account using EC2IAMkeyscanner.py


Update AWS ACCESS_KEY, SECRET_KEY, DocumentHash in the script. That's it; you're all set
